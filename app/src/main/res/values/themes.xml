<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base Theme (No Action Bar) -->
    <style name="Base.Theme.RaktSaathi" parent="Theme.MaterialComponents.Light.NoActionBar">

        <!-- Primary Colors -->
        <item name="colorPrimary">#B71C1C</item>
        <item name="colorPrimaryVariant">#7F0000</item>
        <item name="colorOnPrimary">#FFFFFF</item>

        <!-- Status Bar Color -->
        <item name="android:statusBarColor">#7F0000</item>

        <!-- 🔥 NOTCH FIX -->
        <item name="android:windowLayoutInDisplayCutoutMode">never
        </item>

    </style>

    <!-- App Theme -->
    <style name="Theme.RaktSaathi" parent="Base.Theme.RaktSaathi"/>

</resources>